<article class="post-single-instructor">
    <div class="title">
        <a href="{{post.instructor_url}}" target="_blank">{{instructor.title}}</a>
        <spam>({{post.instructor_url}})</spam>
    </div>
<div class="instructor-post-info">
    by {{post.user.username}} on {{post.created_at}}
    |
    <a href="/post/{{post.id}}">{{post.comments.length}} comment(s)</a>
</div>
</article>

<form class="feedback-form">
    <div>
        <textarea name="feedback-body"></textarea>
    </div>

    <div>
        <button type="add-feedback-btn">add feedback</button>
    </div>
</form>

<div class="feedback">
    {{#each post.comments}}
    <section class="feedback-comments">
        <div class="meta">
            {{user.username}} on {{created_at}}
        </div>
        <div class="text">
            {{comment_text}}
        </div>
    </section>
    {{/each}}
</div>