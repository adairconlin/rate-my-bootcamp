<div class="info">
    <div>
        <p>{{name}}</p>
        <p>Average Rating: {{rating}}/10</p>
    </div>
    <button type="submit" class="review-btn border paragraph">
        <a href="/instructor/{{id}}" class="white">See All Reviews</a>
    </button>
</div>