<div class="review">
    <div class="name-rating">
        <h3>
            <a href="/bootcamp-review/{{id}}">{{bootcamp.name}}</a>
            <span>{{rating}}</span>
        </h3>
    </div>
    <div class="review-text">
        {{review_text}}
    </div>
    <div class="timestamp">
        {{created_at}} by Anonymous
    </div>
</div>